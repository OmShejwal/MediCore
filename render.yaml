services:
  - type: web
    name: vaidyaai-backend
    env: python
    region: oregon
    plan: free
    branch: main
    buildCommand: "pip install -r backend/requirements.txt"
    startCommand: "uvicorn backend.main:app --host 0.0.0.0 --port $PORT"
    envVars:
      - key: GROQ_API_KEY
        value: your-groq-api-key-here
      - key: SECRET_KEY
        value: your-secret-key-change-in-production-09876543210987654321
      - key: DATABASE_URL
        value: sqlite:///./sehatsaathi.db
      - key: JWT_ALGORITHM
        value: HS256
      - key: ACCESS_TOKEN_EXPIRE_MINUTES
        value: 30
      - key: PYTHONUNBUFFERED
        value: "1"
